---
phase: 09-caller-switchover-verify-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/bundle-cli/src/create.rs
  - crates/bundle-cli/src/inspect.rs
autonomous: true
requirements: [VRFY-03]
must_haves:
  truths:
    - "create.rs uses crate::format::format_bytes — no local format_bytes function exists"
    - "inspect.rs uses crate::format::format_bytes — no local format_size function exists"
    - "All existing tests in create.rs and inspect.rs pass without modification"
    - "test_format_size in inspect.rs now implicitly tests crate::format::format_bytes and still passes"
  artifacts:
    - path: "crates/bundle-cli/src/create.rs"
      provides: "Bundle creation with shared format_bytes"
      contains: "use crate::format::format_bytes"
    - path: "crates/bundle-cli/src/inspect.rs"
      provides: "Bundle inspection with shared format_bytes"
      contains: "use crate::format::format_bytes"
  key_links:
    - from: "crates/bundle-cli/src/create.rs"
      to: "crates/bundle-cli/src/format.rs"
      via: "use crate::format::format_bytes"
      pattern: "use crate::format::format_bytes"
    - from: "crates/bundle-cli/src/inspect.rs"
      to: "crates/bundle-cli/src/format.rs"
      via: "use crate::format::format_bytes"
      pattern: "use crate::format::format_bytes"
---

<objective>
Wire create.rs and inspect.rs to use the shared format::format_bytes module from Phase 8, deleting their local duplicate implementations.

Purpose: Eliminate two of three divergent format_bytes/format_size copies (DEDUP-02, DEDUP-03 from Phase 8 scope, but the switchover is Phase 9 work). This contributes to VRFY-03 by confirming all existing tests still pass after the switchover.

Output: Updated create.rs and inspect.rs with no local formatting functions, importing from crate::format instead.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-caller-switchover-verify-decomposition/09-RESEARCH.md

<interfaces>
<!-- Phase 8 provides this module. Verify it exists before proceeding. -->
<!-- If format.rs does not exist or does not export format_bytes, STOP and report. -->

Expected from crates/bundle-cli/src/format.rs (created by Phase 8):
```rust
pub(crate) fn format_bytes(bytes: u64) -> String
// Must support KiB, MiB, GiB, TiB tiers
```

From crates/bundle-cli/src/create.rs (lines 27-41, to be DELETED):
```rust
fn format_bytes(bytes: u64) -> String {
    // No TiB tier — only GiB/MiB/KiB/B
}
// Call site: line 228 — format_bytes(result.size_bytes)
```

From crates/bundle-cli/src/inspect.rs (lines 36-53, to be DELETED):
```rust
pub fn format_size(bytes: u64) -> String {
    // Has TiB tier — this was the canonical version for Phase 8
}
// Call site: line 84 — format_size(manifest.image.size_bytes) becomes format_bytes(...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Precondition — verify Phase 8 modules exist</name>
  <files>crates/bundle-cli/src/format.rs</files>
  <action>
Before making any changes, verify Phase 8 artifacts exist:

1. Check that `crates/bundle-cli/src/format.rs` exists and contains `pub(crate) fn format_bytes`
2. Run `cargo test -p bundle-cli --lib format` to confirm the format module's own tests pass
3. If format.rs does not exist or fails tests, STOP and report: "Phase 8 prerequisite not met — format.rs missing or broken"

Do NOT modify any files in this task. This is a verification-only step.
  </action>
  <verify>
    <automated>cargo test -p bundle-cli --lib format 2>&1 | tail -5</automated>
  </verify>
  <done>format.rs exists, exports format_bytes, and its tests pass. Executor has confirmed Phase 8 prerequisite is met.</done>
</task>

<task type="auto">
  <name>Task 2: Switch create.rs and inspect.rs to shared format_bytes</name>
  <files>crates/bundle-cli/src/create.rs, crates/bundle-cli/src/inspect.rs</files>
  <action>
**create.rs changes:**
1. Add `use crate::format::format_bytes;` to the imports (near the top, after existing `use crate::` imports)
2. Delete the local `fn format_bytes(bytes: u64) -> String` function (lines 27-41 in current source — the function with only GiB/MiB/KiB/B tiers, no TiB)
3. The call site at the `format_bytes(result.size_bytes)` line stays unchanged — it now resolves to `crate::format::format_bytes`
4. Verify no other references to the deleted function exist in create.rs

**inspect.rs changes:**
1. Add `use crate::format::format_bytes;` to the imports
2. Delete the local `pub fn format_size(bytes: u64) -> String` function (lines 36-53 in current source)
3. Change the call site at `format_size(manifest.image.size_bytes)` (line 84) to `format_bytes(manifest.image.size_bytes)`
4. The `test_format_size` test in inspect.rs calls `format_size()` — since that function no longer exists, update the test to call `format_bytes()` from the import. The test assertions remain identical. Change:
   - `assert_eq!(format_size(0), "0 B");` to `assert_eq!(format_bytes(0), "0 B");`
   - Same for all other `format_size(...)` calls in the test — replace with `format_bytes(...)`
   - The test function name `test_format_size` can stay as-is (renaming is optional)

**Important:** The `test_format_size` test includes `format_size(1024u64 * 1024 * 1024 * 1024)` expecting `"1.0 TiB"`. After switching to `crate::format::format_bytes`, this assertion MUST still pass. If it fails, Phase 8's format.rs is missing TiB support — stop and report.

Do NOT touch verify.rs in this task — that is handled in Plan 02.
  </action>
  <verify>
    <automated>cd /Users/ravichillerega/sources/management/os-builder && cargo test -p bundle-cli 2>&1 | tail -20</automated>
  </verify>
  <done>
- create.rs has no local format_bytes function; imports from crate::format
- inspect.rs has no local format_size function; imports format_bytes from crate::format
- All existing tests pass including test_format_size (now testing shared format_bytes with TiB)
- No compiler warnings about unused functions
  </done>
</task>

</tasks>

<verification>
- `cargo test -p bundle-cli` — full test suite passes
- `grep -n "fn format_bytes\|fn format_size" crates/bundle-cli/src/create.rs crates/bundle-cli/src/inspect.rs` — returns no matches (local copies deleted)
- `grep -n "use crate::format::format_bytes" crates/bundle-cli/src/create.rs crates/bundle-cli/src/inspect.rs` — returns matches in both files
</verification>

<success_criteria>
- Zero local format_bytes/format_size functions in create.rs and inspect.rs
- Both files import from crate::format::format_bytes
- All existing tests pass (including test_format_size with TiB assertion)
- No compiler warnings
</success_criteria>

<output>
After completion, create `.planning/phases/09-caller-switchover-verify-decomposition/09-01-SUMMARY.md`
</output>
