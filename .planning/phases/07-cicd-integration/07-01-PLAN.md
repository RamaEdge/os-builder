---
phase: 07-cicd-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
autonomous: true
requirements:
  - CI-01
  - CI-02

must_haves:
  truths:
    - "`make bundle-cli` builds the release binary at `crates/bundle-cli/target/release/edgeworks-bundle` without errors"
    - "`make bundle-cli-test` runs all unit and integration tests in the bundle-cli crate and exits 0"
    - "Both targets appear in the Makefile help output"
  artifacts:
    - path: "Makefile"
      provides: "bundle-cli and bundle-cli-test Make targets"
      contains: "bundle-cli"
  key_links:
    - from: "Makefile"
      to: "crates/bundle-cli/Cargo.toml"
      via: "cargo --manifest-path"
      pattern: "cargo.*--manifest-path crates/bundle-cli/Cargo.toml"
---

<objective>
Add `bundle-cli` and `bundle-cli-test` Makefile targets to the existing os-builder Makefile.

Purpose: Provide standard Make entry points for building and testing the bundle CLI, used both locally and by the CI pipeline.
Output: Updated Makefile with two new phony targets.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/bundle-cli-design.md

Design doc section 7 specifies the exact Makefile targets:

```makefile
.PHONY: bundle-cli
bundle-cli:
	cargo build --release --manifest-path crates/bundle-cli/Cargo.toml

.PHONY: bundle-cli-test
bundle-cli-test:
	cargo test --manifest-path crates/bundle-cli/Cargo.toml
```

The existing Makefile uses this pattern for `.PHONY` declarations — a group declaration at the top plus individual targets. The new targets should be added as a new section following the existing code style.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bundle-cli Makefile targets</name>
  <files>Makefile</files>
  <action>
Add a new section to the Makefile after the "Installation" section. Follow the existing section style with comment banners.

Add these exact lines:

```makefile
# =============================================================================
# Bundle CLI (edgeworks-bundle)
# =============================================================================
.PHONY: bundle-cli bundle-cli-test

bundle-cli:
	@echo "Building edgeworks-bundle (release)..."
	cargo build --release --manifest-path crates/bundle-cli/Cargo.toml
	@echo "Binary: crates/bundle-cli/target/release/edgeworks-bundle"

bundle-cli-test:
	@echo "Testing edgeworks-bundle..."
	cargo test --manifest-path crates/bundle-cli/Cargo.toml
```

Also update the `.PHONY` line near the top of the file to include `bundle-cli bundle-cli-test` in the common targets declaration.

Update the `help` target to include a "Bundle CLI" line:
```
@echo "Bundle CLI: bundle-cli, bundle-cli-test"
```

Add this line after the existing "Install:" line in the help target.
  </action>
  <verify>
Run:
1. `grep -n 'bundle-cli' Makefile` — should show the new targets and phony declarations
2. `make -n bundle-cli` — dry-run should show the cargo build command
3. `make -n bundle-cli-test` — dry-run should show the cargo test command
4. `make help` — should list the Bundle CLI targets
  </verify>
  <done>
- `make -n bundle-cli` outputs `cargo build --release --manifest-path crates/bundle-cli/Cargo.toml`
- `make -n bundle-cli-test` outputs `cargo test --manifest-path crates/bundle-cli/Cargo.toml`
- `make help` shows Bundle CLI targets
  </done>
</task>

</tasks>

<verification>
1. `make -n bundle-cli` shows the cargo build --release command
2. `make -n bundle-cli-test` shows the cargo test command
3. `make help | grep -i bundle` shows Bundle CLI in help output
4. No syntax errors: `make -n help` exits 0
</verification>

<success_criteria>
- CI-01: `make bundle-cli` target exists and invokes `cargo build --release --manifest-path crates/bundle-cli/Cargo.toml`
- CI-02: `make bundle-cli-test` target exists and invokes `cargo test --manifest-path crates/bundle-cli/Cargo.toml`
</success_criteria>

<output>
After completion, create `.planning/phases/07-cicd-integration/07-01-SUMMARY.md`
</output>
