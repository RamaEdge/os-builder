use crate::error::BundleError;

pub fn run(image: &str, output: &std::path::Path, notes: &str, target_device: &str) -> Result<(), BundleError> {
    let _ = (image, output, notes, target_device);
    todo!("not yet implemented")
}
